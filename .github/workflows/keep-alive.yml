name: Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend (keeps Render + Supabase alive)
        run: curl -sf https://klang-valley-choo-choo.onrender.com/api/stations?limit=1 > /dev/null

